type dataItem {
    _id: ID!,
    _name: String,
    _value: String

}
type Query {
    "Retorna live onlines"
        liveDataGroup: [dataItem]

    "Return a single DataItem"
        getDataItem(id: ID!): dataItem

        offlineData: String
}